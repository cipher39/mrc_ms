spring.application.name=MRC-Roadways

author.name="cipher1"


# MySQL settings
#Change these settings according to database you are using
#mysql> SHOW GLOBAL VARIABLES LIKE 'PORT';
#spring.datasource.url=jdbc:mysql://localhost:3306/mrc_db
#spring.datasource.username=root
#If MySQL installation is password proctored,then use below property to set password
#spring.datasource.password=mysql@root

#JPA settings
# It Will show all the SQL Statements that was executed by the JPA
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# Mention the URI of config server in bootstrap.properties file
#spring.cloud.config.uri=http://localhost:1111
spring.config.import=optional:configserver:http://localhost:1111
